// import { logger } from "../../utils/logger.js";
// import { LeaveRepository } from "./leave.repository.js";

// export class Leaveservice{
//     constructor(private readonly repository: LeaveRepository){}
//     async applyForLeave(userId:string,leavedata:any){
//         logger.info(`User ${userId} applying for leave from ${leavedata.fromDate} to ${leavedata.toDate}`)
//    // Business Logic if (balance < 0) throw new Error("Not enough balance");
//        const newleave=
//        await this.repository.createLeave({
//             userId:userId,
//             departmentId:leavedata.departmentId,
//             leaveTypeId: leavedata.leaveTypeId,
//             fromDate: new Date(leavedata.fromDate),
//             toDate: new Date(leavedata.toDate),
//             purpose: leavedata.purpose,
//          })
//     return newleave
//     }
 


// }


